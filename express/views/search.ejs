<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Search Result</title>
</head>
<body>
  <div>
    <h1>Twitter result for <%= search %></h1>
    <% if (result.length) { %>
      <ul>
        <% for (var i = 0; i < result.length; i++) { %>
          <li>
            <%= result[i].text %> - 
            <em><%= result[i].from_user %></em>
          </li>
        <% } %>
      </ul>
    <% } else { %>
      <p>not result</p>
    <% } %>
  </div>
</body>
</html>